---
phase: 01-crashes-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Labby/Views/Core/HomeView.swift
autonomous: true
requirements:
  - CRASH-01
  - CRASH-02
  - NAV-01
must_haves:
  truths:
    - "Tapping Add in AddWidgetView never crashes regardless of optional state"
    - "Only one toolbar appears on the AddWidgetView form"
    - "Navigating from a widget card on the home grid opens the correct detail view without double-push"
    - "HomeWidgetCard is a pure display component with no NavigationLink"
  artifacts:
    - path: "Labby/Views/Core/HomeView.swift"
      provides: "Safe AddWidgetView toolbar and clean navigation ownership"
      contains: "if let cfg = selectedConfig"
  key_links:
    - from: "HomeGridView"
      to: "HomeWidgetCard"
      via: "NavigationLink wrapping in HomeGridView, not inside HomeWidgetCard"
      pattern: "NavigationLink.*HomeWidgetCard"
    - from: "HomeWidgetCard.body"
      to: "widgetContent"
      via: "Direct render without NavigationLink when not editing"
      pattern: "widgetContent"
---

<objective>
Fix crash-causing force-unwraps in AddWidgetView and eliminate double-nested NavigationLink in HomeWidgetCard.

Purpose: The outer toolbar on AddWidgetView uses force-unwraps (`selectedServiceId!`, `selectedConfig!`) that crash when optionals are nil, and duplicates the inner toolbar with divergent logic. HomeWidgetCard wraps its body in NavigationLink, but HomeGridView also wraps HomeWidgetCard in NavigationLink, causing double-push navigation.

Output: HomeView.swift with a single safe toolbar on AddWidgetView and HomeWidgetCard as a pure display component (no NavigationLink).
</objective>

<execution_context>
@/Users/ryanwiecz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanwiecz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Labby/Views/Core/HomeView.swift

<interfaces>
<!-- Key structures the executor needs to understand -->

From HomeView.swift — AddWidgetView has TWO toolbars:
1. Inner toolbar on Form (lines 1508-1559): Uses safe `if let cfg = selectedConfig`, calls `widget.normalizeColumnForSize()`, handles per-kind metric selection properly. This is the KEEPER.
2. Outer toolbar on NavigationView (lines 1566-1601): Uses `selectedServiceId!` and `selectedConfig!` (CRASH), skips `normalizeColumnForSize()`. This must be DELETED.

The outer toolbar also sets `.navigationTitle("Add Widget")` (line 1566) and `.navigationBarTitleDisplayMode(.inline)` (line 1568). These modifiers must be moved to the Form if not already present there.

From HomeView.swift — HomeWidgetCard.body (lines 333-346):
```swift
var body: some View {
    Group {
        if isEditing {
            widgetContent.onTapGesture { onEdit() }
        } else {
            NavigationLink(destination: destinationView(for: config)) {
                widgetContent
            }
            .buttonStyle(.plain)
        }
    }
}
```
The `NavigationLink` here causes double-push because HomeGridView (lines 722-864) ALSO wraps HomeWidgetCard in NavigationLink for each service kind.

From HomeView.swift — HomeWidgetCard.destinationView(for:) (lines 575-586):
```swift
private func destinationView(for config: ServiceConfig) -> some View {
    switch config.kind {
    case .proxmox: ProxmoxView(config: config)
    case .jellyfin: JellyfinView(config: config)
    case .qbittorrent: QBittorrentView(config: config)
    case .pihole: PiHoleView(config: config)
    }
}
```
This function becomes unused after the fix (HomeGridView already has per-kind routing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unsafe outer toolbar from AddWidgetView and fix navigation title placement</name>
  <files>Labby/Views/Core/HomeView.swift</files>
  <action>
In AddWidgetView, delete the outer toolbar block that lives on the NavigationView wrapper. Specifically:

1. Find the closing brace of the Form's `.onAppear` block (around line 1565) — this ends the Form.
2. DELETE everything from line 1566 through line 1601 — the outer `.navigationTitle("Add Widget")`, `.navigationBarTitleDisplayMode(.inline)`, and the outer `.toolbar { ... }` block containing force-unwraps (`selectedServiceId!` on line 1577, `selectedConfig!` on line 1583).
3. On the INNER Form (which already has `.navigationTitle("Add Widget")` at line 1508 and its own safe `.toolbar` at lines 1509-1558), add `#if os(iOS) .navigationBarTitleDisplayMode(.inline) #endif` if it's not already there. Check: the inner Form at line 1508 already has `.navigationTitle("Add Widget")` — just ensure `.navigationBarTitleDisplayMode(.inline)` is also present on the Form, not only on the NavigationView.

The inner toolbar (lines 1509-1558) is the safe implementation that uses `if let cfg = selectedConfig` guard and calls `widget.normalizeColumnForSize()`. It must be preserved exactly as-is.

After this change, there should be exactly ONE `.toolbar` modifier on AddWidgetView, and it should contain `if let cfg = selectedConfig` (never force-unwrap).
  </action>
  <verify>
    <automated>cd /Users/ryanwiecz/Documents/Code/Labby && xcodebuild -project Labby.xcodeproj -scheme Labby -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>
- AddWidgetView has exactly one toolbar (the inner safe one)
- No force-unwraps (`!`) exist in any AddWidgetView toolbar action
- `normalizeColumnForSize()` is called before `onAdd(widget)`
- The project builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove NavigationLink from HomeWidgetCard and clean up unused destinationView</name>
  <files>Labby/Views/Core/HomeView.swift</files>
  <action>
In HomeWidgetCard.body (around lines 333-346), replace the current implementation so that HomeWidgetCard NEVER creates a NavigationLink. The card should always render `widgetContent` directly:

1. Replace the body of HomeWidgetCard to:
```swift
var body: some View {
    widgetContent
        .contentShape(Rectangle())
        .onTapGesture {
            if isEditing {
                onEdit()
            }
        }
}
```

Wait — the non-editing tap behavior needs to be handled by the NavigationLink in HomeGridView, which already wraps HomeWidgetCard. So HomeWidgetCard just needs to show `widgetContent` without any navigation or tap handling for the non-editing case. For editing mode, the `onTapGesture` is needed.

Actually, re-examining: HomeGridView already wraps HomeWidgetCard in NavigationLink when `!isEditingLayout`, and in ReorderableCard when `isEditingLayout`. The ReorderableCard uses its own gesture handling. So HomeWidgetCard should just render `widgetContent` unconditionally:

```swift
var body: some View {
    widgetContent
}
```

The editing tap (`onEdit`) is handled by the `isEditing` branch in HomeGridView which uses `ReorderableCard` — but check: in HomeGridView's editing branch, HomeWidgetCard does NOT get a NavigationLink OR an onTapGesture from GridView. The `onEdit` is passed as a parameter. Looking at the current code, the `onTapGesture { onEdit() }` inside HomeWidgetCard's editing branch IS the only way editing taps are handled. So keep the editing tap gesture:

```swift
var body: some View {
    if isEditing {
        widgetContent
            .onTapGesture { onEdit() }
    } else {
        widgetContent
    }
}
```

2. Delete the `destinationView(for:)` private function (lines 575-586) from HomeWidgetCard since it becomes unused. HomeGridView already has its own per-kind routing with NavigationLink destinations.

3. Verify that HomeGridView's navigation wrapping (lines 722-864) remains intact — it already correctly wraps HomeWidgetCard in NavigationLink for each service kind (qbittorrent, pihole, proxmox) and falls through to plain HomeWidgetCard for jellyfin/others. Do NOT modify HomeGridView.

Note: The `.buttonStyle(.plain)` that was on HomeWidgetCard's NavigationLink is no longer needed inside HomeWidgetCard. The NavigationLinks in HomeGridView already have their own styling.
  </action>
  <verify>
    <automated>cd /Users/ryanwiecz/Documents/Code/Labby && xcodebuild -project Labby.xcodeproj -scheme Labby -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>
- HomeWidgetCard.body contains no NavigationLink
- HomeWidgetCard has no `destinationView(for:)` method
- HomeGridView still wraps HomeWidgetCard in NavigationLink for each service kind
- Tapping a widget card navigates to the correct detail view (single push, no double-nesting)
- The project builds without errors
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `xcodebuild -project Labby.xcodeproj -scheme Labby -destination 'platform=iOS Simulator,name=iPhone 16' build`
2. Grep confirms no force-unwraps in AddWidgetView toolbar: `grep -n 'selectedServiceId!' Labby/Views/Core/HomeView.swift` returns no matches
3. Grep confirms no NavigationLink in HomeWidgetCard body: `grep -A5 'struct HomeWidgetCard' Labby/Views/Core/HomeView.swift` shows no NavigationLink in body
4. Grep confirms exactly one `.toolbar` on AddWidgetView: count of `.toolbar {` between AddWidgetView struct and its closing should be 1
</verification>

<success_criteria>
- Zero force-unwraps in AddWidgetView toolbar code
- Single toolbar on AddWidgetView (the safe inner one with if-let guard)
- HomeWidgetCard renders content without NavigationLink
- HomeGridView retains its per-kind NavigationLink wrapping
- Project compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-crashes-navigation/01-01-SUMMARY.md`
</output>
