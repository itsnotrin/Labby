---
phase: 01-crashes-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Labby/Services/Proxmox/Proxmox.swift
autonomous: true
requirements:
  - NAV-02
must_haves:
  truths:
    - "ProxmoxDetailView opens inside the existing navigation context without double navigation bars"
    - "ProxmoxVMsView opens inside the existing navigation context and its NavigationLink destinations still work"
    - "ProxmoxContainersView opens inside the existing navigation context and its NavigationLink destinations still work"
  artifacts:
    - path: "Labby/Services/Proxmox/Proxmox.swift"
      provides: "Proxmox views without nested NavigationStack"
  key_links:
    - from: "ProxmoxView"
      to: "ProxmoxDetailView"
      via: "NavigationLink push (line 438)"
      pattern: "NavigationLink.*ProxmoxDetailView"
    - from: "NavigationGridSection"
      to: "ProxmoxVMsView / ProxmoxContainersView"
      via: "navigationDestination(for: String.self)"
      pattern: "navigationDestination.*String.self"
---

<objective>
Remove nested NavigationStack from ProxmoxDetailView, ProxmoxVMsView, and ProxmoxContainersView.

Purpose: These three views each create their own NavigationStack, but they are navigation destinations pushed inside an existing NavigationView from HomeView (line 62). The nested NavigationStack causes double navigation bars and broken back-navigation.

Output: Proxmox.swift with all three views using the parent navigation context instead of creating their own.
</objective>

<execution_context>
@/Users/ryanwiecz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanwiecz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Labby/Services/Proxmox/Proxmox.swift

<interfaces>
<!-- Navigation hierarchy the executor needs to understand -->

Navigation chain:
1. HomeView.body (line 62): `NavigationView { ... }` — this is the navigation ROOT
2. HomeGridView wraps widget cards in `NavigationLink { ProxmoxView(config:) }` — push #1
3. ProxmoxView.body (line 387): `ScrollView` with a `NavigationLink { ProxmoxDetailView(...) }` at line 438 — push #2
4. ProxmoxDetailView.body (line 668-669): `NavigationStack { ScrollView { ... } }` — BUG: creates nested navigation
5. NavigationGridSection (line 1068): Uses `NavigationLink(value: "vms")` and `.navigationDestination(for: String.self)` to push ProxmoxVMsView/ProxmoxContainersView
6. ProxmoxVMsView.body (line 1440): `NavigationStack { List { ... } }` — BUG: creates nested navigation
7. ProxmoxContainersView.body (line 1496): `NavigationStack { List { ... } }` — BUG: creates nested navigation

ProxmoxVMsView and ProxmoxContainersView use `NavigationLink(value: vmid)` with `.navigationDestination(for: String.self)` for drill-down into ProxmoxVMDetailView. After removing NavigationStack, the `.navigationDestination` modifier must remain — it will register with the parent navigation context.

ProxmoxDetailView uses NavigationGridSection which has its own `.navigationDestination(for: String.self)` modifier. After removing NavigationStack from ProxmoxDetailView, this will register with the parent context correctly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove NavigationStack from ProxmoxDetailView, ProxmoxVMsView, and ProxmoxContainersView</name>
  <files>Labby/Services/Proxmox/Proxmox.swift</files>
  <action>
Make three targeted changes in Proxmox.swift:

**1. ProxmoxDetailView (line 669):**
Replace `NavigationStack {` with just the `ScrollView` content. The body currently is:
```swift
var body: some View {
    NavigationStack {
        ScrollView {
            ...
        }
        .navigationTitle(...)
        .refreshable(...)
    }
}
```

Change to:
```swift
var body: some View {
    ScrollView {
        ...
    }
    .navigationTitle(...)
    .refreshable(...)
}
```

Remove the `NavigationStack {` opening brace (line 669) and its matching closing brace. Keep ALL content inside (ScrollView, .navigationTitle, .refreshable, .toolbar, etc.) — just unwrap them from NavigationStack.

**2. ProxmoxVMsView (line 1440):**
Replace `NavigationStack {` with just the `List` content. The body currently is:
```swift
var body: some View {
    NavigationStack {
        List { ... }
        .navigationDestination(for: String.self) { ... }
        .refreshable { ... }
        .navigationTitle(...)
    }
}
```

Change to:
```swift
var body: some View {
    List { ... }
    .navigationDestination(for: String.self) { ... }
    .refreshable { ... }
    .navigationTitle(...)
}
```

Remove the `NavigationStack {` opening brace (line 1440) and its matching closing brace (line 1457). Keep the `.navigationDestination(for: String.self)` modifier — it must remain so that `NavigationLink(value: vmid)` still works for drilling into ProxmoxVMDetailView.

**3. ProxmoxContainersView (line 1496):**
Same pattern as ProxmoxVMsView. Replace `NavigationStack {` with just the `List` content:
```swift
var body: some View {
    List { ... }
    .navigationDestination(for: String.self) { ... }
    .refreshable { ... }
    .navigationTitle(...)
}
```

Remove the `NavigationStack {` opening brace (line 1496) and its matching closing brace (line 1513). Keep `.navigationDestination(for: String.self)`.

**Important:** Do NOT touch NavigationGridSection — it does not create a NavigationStack, it just uses NavigationLink(value:) and .navigationDestination which correctly participate in the parent navigation context.
  </action>
  <verify>
    <automated>cd /Users/ryanwiecz/Documents/Code/Labby && xcodebuild -project Labby.xcodeproj -scheme Labby -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>
- ProxmoxDetailView.body does not contain NavigationStack
- ProxmoxVMsView.body does not contain NavigationStack
- ProxmoxContainersView.body does not contain NavigationStack
- All .navigationDestination modifiers are preserved for drill-down navigation
- All .navigationTitle modifiers are preserved
- The project builds without errors
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `xcodebuild -project Labby.xcodeproj -scheme Labby -destination 'platform=iOS Simulator,name=iPhone 16' build`
2. Grep confirms no NavigationStack in target views: `grep -n 'NavigationStack' Labby/Services/Proxmox/Proxmox.swift` returns no matches in ProxmoxDetailView, ProxmoxVMsView, or ProxmoxContainersView
3. Grep confirms navigationDestination preserved: `grep -n 'navigationDestination' Labby/Services/Proxmox/Proxmox.swift` still returns matches for VMsView and ContainersView
</verification>

<success_criteria>
- Zero NavigationStack instances in ProxmoxDetailView, ProxmoxVMsView, and ProxmoxContainersView
- navigationDestination modifiers preserved for drill-down into ProxmoxVMDetailView
- navigationTitle modifiers preserved on all views
- Project compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-crashes-navigation/01-02-SUMMARY.md`
</output>
